trigger: none

stages:
  - stage: Destroy
    displayName: Destroy NetProbe
    jobs:
      - job: DestroyNetProbe
        displayName: Remove NetProbe from AKS
        pool:
          name: $(AGENT_POOL)
        steps:
          - checkout: none

          - bash: |
              set -euo pipefail

              LOCAL_BIN="$HOME/.local/bin"
              mkdir -p "$LOCAL_BIN"
              export PATH="$LOCAL_BIN:$PATH"
              echo "##vso[task.prependpath]$LOCAL_BIN"

              if ! command -v kubectl &>/dev/null; then
                echo "Installing kubectl..."
                KVER="$(curl -fsSL https://dl.k8s.io/release/stable.txt)"
                curl -fsSLo "$LOCAL_BIN/kubectl" \
                  "https://dl.k8s.io/release/${KVER}/bin/linux/amd64/kubectl"
                chmod +x "$LOCAL_BIN/kubectl"
              fi
              kubectl version --client

              if ! command -v helm &>/dev/null; then
                echo "Installing helm..."
                curl -fsSL https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 \
                  | USE_SUDO=false HELM_INSTALL_DIR="$LOCAL_BIN" bash
              fi
              helm version --short
            displayName: Install kubectl and Helm

          - task: HelmDeploy@0
            displayName: Uninstall NetProbe Release
            continueOnError: true
            inputs:
              connectionType: 'Kubernetes Service Connection'
              kubernetesServiceConnection: $(AKS_SERVICE_CONNECTION)
              namespace: $(NAMESPACE)
              command: uninstall
              arguments: netprobe

          - task: Kubernetes@1
            displayName: Verify NetProbe Resources Removed
            inputs:
              connectionType: 'Kubernetes Service Connection'
              kubernetesServiceEndpoint: $(AKS_SERVICE_CONNECTION)
              namespace: $(NAMESPACE)
              command: get
              arguments: 'pods,daemonsets -l app.kubernetes.io/name=netprobe --ignore-not-found'
              outputFormat: ''
